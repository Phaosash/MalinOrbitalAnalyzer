<Window x:Class="MalinOrbitalAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MalinOrbitalAnalyzer"
        xmlns:vm="clr-namespace:MalinOrbitalAnalyzer.ViewModels"
        xmlns:ctrls="clr-namespace:MalinOrbitalAnalyzer.Views"
        mc:Ignorable="d"
        Title="Malin Orbital Analyzer" Height="768" Width="1360">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--    Application Version details     -->
        <TextBlock Grid.Row="2" FontSize="8"
                   Text="Version: 1.0.5" Margin="5"
                   HorizontalAlignment="Right"/>

        <!--    Application Title     -->
        <TextBlock Grid.Row="0" FontSize="24"
           Text="Marlin Orbital Analyzer" Margin="5" FontWeight="SemiBold"/>
        
        <!--    Main Content Area   -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--    Adds the Combined List View control     -->
            <ctrls:CombinedSensorView Grid.Column="0" />

            <!--    Adds the Control for Sensor A       -->
            <ctrls:SensorAView Grid.Column="1" />

            <!--    Adds the control for Sensor B   -->
            <ctrls:SensorBView Grid.Column="2" />
        </Grid>
    </Grid>
</Window>